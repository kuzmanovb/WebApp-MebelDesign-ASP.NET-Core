
@{ this.ViewData["Title"] = "Thank You"; }

<div class="site-section services-1-wrap m-5">
    <div class="container">
        <div class="text-center border border-dark p-lg-5">
            <h3 class="m-3">Вашата поръчка е приета.<br /> Благодаря ви , че използвахне нашите услуги.</h3>
            <span id="timer">
            </span>
        </div>
    </div>
</div>


@section Scripts
{
    <script type="text/javascript">
        var count = 5;
        var redirect = "/Orders";

        function countDown() {
            var timer = document.getElementById("timer");
            if (count > 0) {
                count--;
                timer.innerHTML = "Вие ще бъдете пренасочени към страница с вашите поръчки" + " " + count + " секунди.";
                setTimeout("countDown()", 1000);
            } else {
                window.location.href = redirect;
            }
        }
        countDown();
    </script>
}