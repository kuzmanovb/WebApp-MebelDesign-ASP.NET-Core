
@{ this.ViewData["Title"] = "Thank You"; }

<div class="site-section services-1-wrap m-5">
    <div class="container">
        <div class="text-center border border-dark p-lg-5">
            <h3 class="m-3">Благодаря ви за проявения интерес към нашата компания</h3>
            <p>Ние ще направим всичко възможно, за да отговори на въпроса ви в най-кратък срок на посоченият от вас email адрес.</p>
            <span id="timer">
            </span>
        </div>
    </div>
</div>


@section Scripts
{
    <script type="text/javascript">
        var count = 5;
        var redirect = "/";

        function countDown() {
            var timer = document.getElementById("timer");
            if (count > 0) {
                count--;
                timer.innerHTML = "Вие ще бъдете пренасочени към началната страница след" + " " + count + " секунди.";
                setTimeout("countDown()", 1000);
            } else {
                window.location.href = redirect;
            }
        }
        countDown();
    </script>
}