using System;
using System.Collections.Generic;
using System.Linq;

namespace GenericCountMethodStrings
{
    class Program
    {
        static void Main(string[] args)
        {
           
            
            
            
        }

        static int Count<T>(List<T> list, T arg)
        {
            int count = 0;
            foreach (var item in list)
            {
                if (arg.CompareTo(itam))
                {

                }
            }

            return count;
        }
    }
}
